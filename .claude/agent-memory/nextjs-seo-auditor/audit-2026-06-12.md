---
name: audit-2026-06-12
description: Second SEO audit pass 2026-06-12 — what this auditor changed (OG images on detail pages, list-page schema, viewport)
metadata:
  type: project
---

Second technical-SEO audit pass on blacklabdev.com, 2026-06-12 (after the earlier same-day pass recorded in user auto-memory `project-seo-audit.md`).

**Why:** User asked to find and implement remaining quick wins not already done.

**How to apply:** Don't re-implement these. The earlier pass + this pass together cover the major structured-data/metadata surface.

## Changed this pass
- `app/layout.tsx`: added `export const viewport` → `themeColor "#0b0b0c"`, `colorScheme "dark"` (was no theme-color tag at all).
- `app/articles/[slug]/page.tsx`: `generateMetadata` now emits full `openGraph` (type article, publishedTime, authors, per-post image from mainImage) + `twitter` summary_large_image. BlogPosting schema gained `inLanguage` + `mainEntityOfPage`.
- `app/case-studies/[slug]/page.tsx`: `generateMetadata` now emits per-study OG/Twitter image from mainImage; Article schema gained `dateModified` (via new `_updatedAt` in GROQ), `inLanguage`, object-form `mainEntityOfPage`.
- `app/articles/page.tsx`: added CollectionPage+Blog+BreadcrumbList JSON-LD.
- `app/case-studies/page.tsx`: added CollectionPage+ItemList+BreadcrumbList JSON-LD.
- `app/page.tsx`: removed invalid `streetAddress: "Cincinnati"`; WebSite schema gained `@id #website`, `publisher` → #business, `inLanguage`.

## Verified clean
- `npx tsc --noEmit` passes after all edits.

## Not changed (intentionally)
- Service detail pages, /industries, /wordpress-development, /services, /contact, /cincinnati-web-developer already had solid metadata + schema from prior pass; left as-is.
- Did NOT invent a street address.

See also [[schema-conventions]].
